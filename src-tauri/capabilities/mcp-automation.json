{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mcp-automation",
  "description": "Debug-only automation access enabled by tauri.mcp.conf.json",
  "windows": ["main"],
  "permissions": ["mcp-bridge:default"]
}
